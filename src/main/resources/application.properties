spring.config.import=env.properties
spring.application.name=EmployeeMgmtPortal

server.port:${SERVER_PORT}

spring.datasource.url=jdbc:postgresql://localhost:${DB_PORT}/${POSTGRES_DATABASE}
spring.datasource.username=${POSTGRES_USERNAME}
spring.datasource.password=${POSTGRES_PASSWORD}
spring.jpa.show-sql=true

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.hibernate.ddl-auto =update

upload-dir=./resumes

#SMTP Credentials
spring.mail.host=webmail.evolvingsols.com
spring.mail.port=587
spring.mail.username=abhijeetgu@evolvingsols.com
spring.mail.password=Cybage@12345#
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=*

spring.main.allow-bean-definition-overriding=true
spring.main.allow-circular-references=true

